<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table class="table">
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Nick name</th>
      <th>Admin</th>
      <th>Moderator</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.first_name %></td>
        <td><%= user.last_name %></td>
        <td><%= user.nick_name %></td>
        <td><%= user.admin %></td>
        <td><%= user.moderator %></td>
        <td><%= link_to 'anzeigen', user, class: 'btn btn-info' %></td>
        <td><%= link_to 'bearbeiten', edit_user_path(user), class: 'btn btn-warning' %></td>
        <td><%= link_to 'löschen', user, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path, class: 'btn btn-success' %>
