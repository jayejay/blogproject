<div class="tags">
  <h3>Häufigste Tags</h3>

  <table>
    <% Tag.joins(:posts).group('tags.id').where(:posts => {:active => true}).order('COUNT(*) DESC').take(5).each do |t| %>
        <tr>
          <td><%= link_to t.name + ' (' + t.posts.where(:posts => {:active => true}).count.to_s + ')', tag_path(t.id),\
            class:'btn btn-success btn-xs btn-round',\
              style: 'background-color:' + t.color + '; color:' + t.text_color + ';' %>
          </td>
        </tr>
    <% end %>
  </table>

  <%= link_to '...mehr', tags_path %>

</div>